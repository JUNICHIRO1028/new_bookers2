<h1>Books</h1>


<% @books.each do |book| %>
    <p>投稿ユーザー画像:<%= image_tag 'sample-author1.jpg'%></p>
    <p>Title:<%= book.title %></p>
    <p>Body:<%= book.body %></p>
    <p>ユーザーネーム:<%= book.user.name %>
    <a><%= link_to "show", book_path(book.id) %></a>
    
<% end %>